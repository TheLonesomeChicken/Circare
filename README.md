## to start api
`fastapi dev searchAPI.py --host 0.0.0.0`

## test the api
`http://192.168.1.220:8000/search?q=google`

`q=google` in this case we are looking for `google` because thats what `q` is set to
